# AIキャラクター占いシステム 事業・技術計画

> このファイルはプロジェクトの進行とともに随時更新する。

## Context

自作キャラクターを活用したAI占いサービスを構築し、継続的な収益を得ることが目標。
プロジェクトは現在ゼロ状態のため、事業設計・技術選定・MVP構築の全フェーズを一から進める。

---

## Phase 0：事業設計（先に決めること）

### 占いの種類を選定
| 種類 | AI親和性 | 差別化しやすさ |
|------|----------|----------------|
| タロット | ◎ | ◎（カード解釈をキャラの口調で） |
| 星占い（西洋占星術） | ○ | △（競合多い） |
| 数秘術 | ◎ | ○（誕生日だけで完結） |
| 四柱推命 | △（計算複雑） | ◎（和風キャラに合う） |
| 手相・夢占い | ○（画像入力活用可） | ○ |

**推奨：タロット + 数秘術をまず実装**（入力がシンプル、AI生成テキストが映える）

### 収益モデル
| モデル | 難易度 | 初期収益 |
|--------|--------|----------|
| 単発課金（1回 ¥300〜500） | 低 | 即時 |
| サブスクリプション（月¥980〜1,980） | 中 | 継続安定 |
| フリーミアム（無料3回→有料） | 低〜中 | ユーザー獲得しやすい |
| キャラクターライセンスB2B | 高 | 大きい |

**推奨：フリーミアム（無料3回 → サブスク月¥980）**で立ち上げ

### キャラクター設計
- 外見・名前・口調を定義する「キャラクター仕様書」を作成
- AIへのシステムプロンプトにキャラ設定を埋め込む
- 将来的に複数キャラ展開（属性違い、占い種類違い）

---

## Phase 1：MVP構築（2〜4週間）

### 目標
「1キャラ・1占い種類・Stripe決済」で動く最小プロダクト

### 技術スタック
```
フロントエンド : Next.js 14 (App Router) + Tailwind CSS
バックエンド   : Next.js API Routes（同一リポジトリ）
AI           : Claude API (claude-sonnet-4-6)
決済         : Stripe（単発課金 + サブスク）
DB           : Supabase（PostgreSQL + Auth）
デプロイ      : Vercel（無料枠で開始）
```

### ファイル構成（予定）
```
uranai-system/
├── app/
│   ├── page.tsx              # ランディングページ
│   ├── uranai/page.tsx       # 占いUI（チャット形式）
│   └── api/
│       ├── fortune/route.ts  # Claude API呼び出し
│       └── stripe/           # 決済webhook
├── lib/
│   ├── claude.ts             # Anthropic SDK wrapper
│   ├── characters/           # キャラクター定義（プロンプト）
│   │   └── character1.ts
│   └── divination/           # 占いロジック
│       ├── tarot.ts
│       └── numerology.ts
├── components/
│   ├── CharacterAvatar.tsx
│   ├── FortuneChat.tsx
│   └── PaywallModal.tsx
└── supabase/
    └── schema.sql
```

### Claude APIの使い方（キャラクター占い）
```typescript
// lib/claude.ts の中核ロジック
const response = await anthropic.messages.create({
  model: "claude-sonnet-4-6",
  system: character.systemPrompt, // キャラ設定＋占い師設定
  messages: [
    { role: "user", content: buildFortunePrompt(input, divinationType) }
  ]
});
```

システムプロンプト例（キャラ定義）：
```
あなたは「ルナ」という神秘的な月の精霊です。
口調は柔らかく、語尾に「〜ですわ」「〜でしょう」をつけます。
タロットカードを引き、その意味を詩的に、しかし具体的なアドバイスを含めて伝えます。
```

### Stripe実装方針
- 無料ユーザー：3回/日まで占い可能（Supabaseで利用回数管理）
- 有料化：`/api/stripe/checkout` でサブスク申込 → webhook で DB にステータス保存
- Stripe Price ID をenv変数で管理

---

## Phase 2：拡張（MVP後）

- キャラクター追加（2〜3体）
- 占い種類追加（星占い、四柱推命）
- 過去の占い履歴表示
- LINE通知 or メール通知（誕生日占いなど）
- SNSシェア機能（口コミ獲得）

---

## Phase 3：スケール

- モバイルアプリ化（React Native / PWA）
- キャラクターボイス（TTS連携）
- 他サイトへのiframe埋め込みライセンス販売

---

## 実装ステップ（Phase 1 詳細）

- [ ] 1. キャラクター仕様書作成（テキスト・画像）→ システムプロンプト化
- [ ] 2. 占いの種類を1つ選ぶ
- [ ] 3. `npx create-next-app` でプロジェクト初期化
- [ ] 4. Supabase プロジェクト作成・スキーマ定義（users, readings, subscriptions）
- [ ] 5. Claude API 統合・タロット占いの基本フロー実装
- [ ] 6. チャット形式のUIコンポーネント実装
- [ ] 7. フリーミアム制限ロジック実装
- [ ] 8. Stripe 決済フロー実装
- [ ] 9. Vercel デプロイ・動作確認
- [ ] 10. Stripe 本番キー切り替え・公開

---

## 必要な準備（開始前チェックリスト）

- [ ] Anthropic API キー取得
- [ ] Stripe アカウント開設（個人 or 法人）
- [ ] Supabase アカウント作成
- [ ] Vercel アカウント作成
- [ ] キャラクターのビジュアル（イラスト）用意
- [ ] キャラクターの名前・口調・世界観を文書化

---

## 収益シミュレーション

| ユーザー数 | 有料転換率 | 月次収益（月¥980） |
|-----------|-----------|-------------------|
| 100人 | 10% | ¥9,800 |
| 500人 | 10% | ¥49,000 |
| 1,000人 | 15% | ¥147,000 |

ユーザー獲得：X（Twitter）でのキャラ投稿 → 占いへの誘導が最も低コスト

---

## 今すぐやること

### Step 1：キャラクター仕様を固める（★最初の着手点）

決めるべき項目：
- 名前・性別・年齢感
- 外見イメージ（人間？精霊？動物？和風？洋風？）
- 口調・一人称・語尾の癖
- 世界観・バックストーリー（どこから来た占い師か）
- 得意な占いの種類（キャラに合わせて決める）

→ この仕様書が決まれば、占いの種類・収益化タイミングも自然に決まる

### Step 2：占いの種類を1つ選ぶ
キャラクターの世界観に合わせて選定後、このファイルを更新する。

### Step 3：アカウント準備（並行して進める）
- Anthropic API キー取得
- Stripe アカウント開設
- Supabase / Vercel アカウント作成

### Step 4：Next.js プロジェクト初期化
```powershell
cd C:\Users\81907\Desktop\uranai-system
npx create-next-app@latest . --typescript --tailwind --app
```

---

## 収益化タイミングの考え方

実装内容によって判断：

- **最速（1〜2ヶ月）**: タロット1種 × 1キャラのみ → Stripe単発課金（¥300/回）
- **中間（3ヶ月）**: 2〜3種の占い × 1〜2キャラ → フリーミアム＋サブスク
- **じっくり（6ヶ月）**: キャラ複数・SNS連携・履歴機能込みで完成度高く公開
