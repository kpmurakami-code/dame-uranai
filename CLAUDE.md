@AGENTS.md

---

# サブエージェント ワークフロー

## 構成

このプロジェクトは 3 つのサブエージェントで開発を進める。

| エージェント | 役割 | 起動タイミング |
|-------------|------|---------------|
| **planner** | 短いアイデア → `SPEC.md` に展開 | 新機能・新製品の設計開始時 |
| **generator** | `SPEC.md` を読み、1スプリント実装 | 「スプリント N を実装して」と指示されたとき |
| **evaluator** | Playwright MCP でアプリを操作しテスト | generator の引き渡しレポートが出たとき |

---

## 起動ルール

### planner を呼ぶとき
- ユーザーが新しい機能・製品を説明したとき（1〜4行のアイデア）
- 既存の `SPEC.md` を大幅に変更する必要があるとき

### generator を呼ぶとき
- ユーザーが「スプリント N を実装して」と指示したとき
- **必ず `SPEC.md` が存在することを確認してから呼ぶ**
- スプリントは必ず番号順に進める（スキップ・先取り禁止）

### evaluator を呼ぶとき
- generator が「引き渡しレポート」を出力したとき（自動的に呼ぶ）
- ユーザーが「テストして」「評価して」と指示したとき

---

## ハンドオフ プロトコル

```
ユーザー
  │
  ▼ アイデア・仕様変更
planner ──→ SPEC.md を書き出す
  │
  ▼ 「スプリント N を実装」
generator ──→ コードを実装 ──→ 引き渡しレポートを出力
                                    │
                                    ▼ 自動的に
                              evaluator ──→ Playwright MCP でアプリを操作
                                    │
                         ┌──────────┴──────────┐
                      PASS                    FAIL
                         │                    │
                    次のスプリントへ      generator にフィードバック
                                              │
                                         再実装 → 再評価
```

---

## SPEC.md の扱い

- **`SPEC.md` はプロジェクト全体の唯一の真実（Single Source of Truth）**
- generator は `SPEC.md` に書かれていないことを実装しない
- 仕様が変わったとき：必ず `SPEC.md` の該当箇所と変更履歴セクションを更新してから generator を呼ぶ
- `SPEC.md` がない状態で generator を起動してはいけない

---

## イテレーション ルール（FAIL 時）

1. evaluator が FAIL を宣言したら、そのレポートを generator に渡す
2. generator は **新しいスプリントとして扱わず**、同じスプリントの修正として対応する
3. 修正完了後、generator は再度引き渡しレポートを出力する
4. evaluator が再テストを実施する
5. **FAIL が 3 回続いたら**：ユーザーに状況を報告し、方針を確認する

---

## evaluator の必須ルール

- テストは **必ず Playwright MCP を使って実際にアプリを操作する**（コードを読むだけは不可）
- スナップショットモード（`browser_snapshot`）でアクセシビリティツリーを確認する
- `browser_console_messages` でランタイムエラーを必ず確認する
- 前スプリントの後退チェック（回帰テスト）は毎回必須

---

## 全エージェント共通ルール

- **このプロジェクトの Next.js は通常版と異なる**（`@AGENTS.md` を参照）
- セッション中に学んだ重要な制約（例：`proxy.ts` を使うこと）は SPEC.md または CLAUDE.md に記録する
- ユーザーへの報告はスコア表を含むシンプルなサマリーにする
