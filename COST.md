# ダメ占い コスト試算メモ

> 最終更新：2026-05-30  
> 前提：為替 **¥150/$**、Claude **Sonnet 4.6**（入力 $3 / 出力 $15 / 5分キャッシュ書込 $3.75 / キャッシュ読込 $0.30・per MTok）。料金は 2026-05-30 時点の Anthropic / Vercel / Supabase 公開料金にもとづく概算。**トークン数は実装からの見積りであり、公開後に Anthropic Console の実測値で更新すること。**

---

## コストは3本柱（＋ドメイン）

| 費目 | 役割 | 課金の型 |
|------|------|----------|
| **Anthropic API** | 占い文の生成（`/api/fortune` `/api/numerology` `/api/daily`、いずれも Sonnet 4.6） | **従量**（利用量比例＝変動費の主役） |
| **Vercel** | ホスティング | 固定（Pro $20/月〜）＋従量 |
| **Supabase** | 認証（Google OAuth）・占い履歴DB | 固定（Free or Pro $25/月） |
| ドメイン | dame-uranai.com | 年 ¥3,000 前後（≒ ¥250/月） |

---

## ① Anthropic API（変動費・最重要）

1回あたりの概算（プロンプトキャッシュ込み・Sonnet 4.6）:

| 機能 | 入力(目安) | 出力(目安) | 1回あたり |
|------|-----------|-----------|-----------|
| タロット占い (fortune) | system 〜1,500tok（キャッシュ）＋user 150 | 〜800tok | **約 $0.018（¥2.7）** |
| 数秘術 (numerology) | 〜1,300tok | 〜900tok | **約 $0.018（¥2.7）** |
| 今日のひとこと (daily) | 〜800tok | 〜200tok | 約 $0.006（¥0.9） |

→ **占い1回 ≒ ¥2〜3**（トラフィック増でキャッシュが効くと ¥2 前後に低下）。

**月間ボリューム別（占い回数）:**

| 規模 | 占い/月 | Anthropic概算/月 |
|------|---------|------------------|
| 小（公開直後） | 1,000回 | 約 $18（**¥2,700**） |
| 中（伸び始め） | 10,000回 | 約 $180（**¥27,000**） |
| 大（人気化） | 100,000回 | 約 $1,800（**¥270,000**） |

> 「今日のひとこと」は日付で決定的なので、**日次キャッシュ**すれば人数に依存せず月 ¥30 程度に圧縮可能（現状は閲覧ごとに生成し得る＝改善ポイント）。

---

## ② Vercel（ホスティング）

- **Hobby（無料）**：100GB帯域 / 関数呼び出し1M / 画像変換5,000。ただし**非商用限定**（商用化したら Pro 必須）。
- **Pro：$20/月/人（約 ¥3,000）** ＋従量。1TB帯域・関数呼び出し無制限（超過 $0.60/1M）・エッジ10M・画像変換超過 $0.05/1K。
- 本アプリの規模なら**中規模まで Pro 基本料 $20 でほぼ収まる**見込み。注視点：関数実行時間（AI生成）・画像最適化。

## ③ Supabase（認証・DB／見落としがちな固定費）

- **Free**：認証5万MAU・DB500MB 等。**無料プロジェクトは一定期間アクセスが無いと自動停止**＝本番不向き。
- **Pro：$25/月（約 ¥3,750）**：停止なし・バックアップ等。**商用運用では実質 Pro 必須**。

---

## 月額トータルの目安

| フェーズ | 固定費 | AI（変動） | 合計の目安 |
|---------|--------|-----------|-----------|
| **準備中（無料・非商用）** | ¥0（Vercel Hobby＋Supabase Free）＋ドメイン¥250 | 使った分だけ（少額） | **〜¥1,000/月** |
| **商用・小規模**（1,000占い/月） | Vercel Pro ¥3,000＋Supabase Pro ¥3,750＋ドメイン¥250 ≈ **¥7,000** | ¥2,700 | **約 ¥10,000/月** |
| **商用・中規模**（10,000占い/月） | ≈ ¥7,000（＋小超過） | ¥27,000 | **約 ¥35,000/月** |
| **商用・人気化**（100,000占い/月） | ¥7,000〜（帯域/関数超過で増） | ¥270,000 | **¥280,000〜/月** |

**ポイント**
- 固定費は商用化で **約 ¥7,000/月** とほぼ一定。
- スケール時に効くのは **Anthropic API（占い回数比例）**。
- **占い1回 ¥2〜3** が原価の目安 → 課金単価（Sprint 5 Stripe）をこれ以上に設計すれば原価割れしない（例：サブスク ¥980 で月数十回なら十分黒字）。

---

## コストを抑えるレバー（効果順）

1. **今日のひとことを日次キャッシュ**（人数非依存・即効）
2. **daily を Haiku 4.5 に変更**（$1/$5＝Sonnetの約1/3。短文なので品質十分）
3. **プロンプトキャッシュ拡大**（fortune は実装済み。numerology / daily にも `cache_control` 付与で入力コスト減）
4. 出力 `max_tokens` の適正化（冗長生成防止）
5. **フリーミアムのサーバー側制限**（無料枠の無制限消費＝API浪費を防ぐ。現状は localStorage 依存で回避可能）

---

## プラン移行の方針（メモ）

- Vercel：**Hobby（無料・非商用）→ 集客 or 課金の直前に Pro へ**（Hobby は規約上 非商用限定）。
- Supabase：本番化したら **Pro**（無料プロジェクトの自動停止を回避）。
- 課金（Stripe / Sprint 5）：公開後の利用データを見て投入時期を判断（[SPEC 変更履歴 v1.5] 方針）。
